# 第 4 章 本地模型与本地算力

本章状态：结构稿

## 4.1 什么是本地算力

- 计算发生在哪里。
- 数据经过哪些设备。
- 本地运行和离线运行的区别。
- 本地设备的持续成本。

## 4.2 本地模型需要什么硬件

- CPU。
- GPU。
- 内存。
- 显存和共享内存。
- 存储。

## 4.3 模型大小和量化

- 参数量。
- 量化。
- 上下文长度。
- 输出速度。
- 任务质量。

## 4.4 本地模型运行环境

- Ollama。
- Open WebUI。
- 模型文件。
- 服务接口。
- 数据目录。

## 4.5 哪些任务适合本地

- 重复处理。
- 私密文档。
- 离线需求。
- 固定分类和提取。
- 本地草稿。

## 4.6 哪些任务继续使用云端

- 低频任务。
- 需要更强推理的任务。
- 本地设备无法满足速度和质量的任务。
- 依赖云端工具的任务。

## 4.7 混合方式

混合工作流可以按下面的顺序运行。

```text
本地接收和整理输入
→ 按任务选择本地模型或云端模型
→ 人工检查结果
→ 保存结果和运行记录
```

本地模型可以处理重复分类、内容提取、私密文档和草稿。云端模型可以处理本地设备暂时达不到质量要求的任务。编程工具和其他专用工具也可以放在这一步，但每个工具只获得完成当前任务所需的目录和权限。

模型选择需要记录任务、模型版本、等待时间、资源使用和人工检查结果。某个模型通过一次任务，只说明这次检查完成。它不能直接证明其他任务也适合，也不能推出长期费用和稳定性结论。

发送邮件、邀请成员、发布内容和执行管理员动作放在人工确认之后。模型生成了动作建议，不等于动作已经执行。

## 4.8 多个 Agent 怎样共享模型

多个 Agent 可以共用一个本地模型服务，不需要为每个岗位重复加载同一个模型。Agent 提交任务，调度队列再根据任务类型选择大模型、小模型或云端模型。

即时对话、大模型任务、短提取任务和浏览器任务使用不同队列。并发上限由队列管理，不由岗位 Agent 自己修改。队列还要给即时任务、故障处理和模型重载留下资源。

测试时一起记录首字等待时间、总吞吐、可用内存、Swap、失败和模型重载。短请求在某个并发数下完成，只能说明这批短请求通过。长期运行还要继续观察等待时间、失败率和人工接管。

## 4.9 怎样测试

使用同一批任务记录模型、版本、时间、资源和人工检查结果。
