# 这台机器上和本地模型有关的两条防火墙规则。
#
# 这个文件不会被执行，是给你照抄的。
# 地址段一律换成你自己家里那个，下面写的 192.168.1.0/24 是示例。
# 教材第 3 卷第 9 章、第 6 卷第 5 章、第 8 章。
#
# ⚠️ 先确认 SSH 那条规则已经在了，再动这里的东西。
#    远程管理那一套在 labs/level-30/01-remote-managed-box。

# ---------------------------------------------------------------- 1
# 放行 Open WebUI，家里人从手机和笔记本进的那个门。

sudo ufw allow from 192.168.1.0/24 to any port 3080 proto tcp

# ---------------------------------------------------------------- 2
# 放行 Ollama 的接口，局域网里的程序和脚本连的那个门。
# 只有一台机器要连的话，把网段换成那一台的地址，例如 192.168.1.77。
# 这比开整段紧，值得做。

sudo ufw allow from 192.168.1.0/24 to any port 11434 proto tcp

# ---------------------------------------------------------------- 3
# 念一遍规则表。每一行你都该说得出这个口是给谁开的。

sudo ufw status verbose

# 配好之后，表里和本地模型有关的应该是这几行：
#
#   To                         Action      From
#   --                         --          ----
#   3080/tcp                   ALLOW IN    192.168.1.0/24
#   11434/tcp                  ALLOW IN    172.17.0.0/16
#   11434/tcp                  ALLOW IN    192.168.1.0/24
#
# 172.17 那一行是给 Docker 容器开的（教材第 6 卷第 4 章）。
#
# ⚠️ Docker 发布的端口是自己去改系统底层转发规则实现的，它插在 ufw 前面。
#    所以 3080 就算不写这条规则，局域网里照样连得上。
#    写它是为了让这张表完整——表不完整，你就管不住这台机器上开了哪些口。
#
# ⚠️ 这两个口都没有认证。11434 连登录页都没有。
#    只在局域网里开，不要在路由器上给它们做端口转发。
#    出门要用走 Tailscale（教材第 3 卷第 6 章）。
