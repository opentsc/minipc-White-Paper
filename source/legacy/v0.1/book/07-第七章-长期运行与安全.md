# 第 7 章 长期运行与安全

本章状态：结构稿

## 7.1 服务清单

- 服务名称。
- 端口。
- 数据目录。
- 日志。
- 启动方式。
- 负责人。

## 7.2 网络边界

- 局域网。
- 私有网络。
- 公网。
- 域名和证书。
- 端口和防火墙。

## 7.3 账户和密钥

- 普通账户。
- 管理员权限。
- SSH 密钥。
- API 密钥。
- 环境变量。

## 7.4 更新

- Ubuntu 更新。
- Docker 镜像更新。
- 模型更新。
- 更新前备份。
- 更新后检查。
- 回滚。

## 7.5 备份和恢复

- 备份什么。
- 保存在哪里。
- 保存多久。
- 怎样恢复。
- 怎样验证恢复成功。

## 7.6 监控

- 服务是否在线。
- CPU、内存和磁盘。
- 日志。
- 通知。

## 7.7 故障处理

- 从用户现象开始。
- 检查服务和端口。
- 查看日志和资源。
- 回看最近修改。
- 没有恢复点时停止操作。

## 7.8 独立 Agent 的权限

同一台 Mini PC 可以运行多个服务。Agent 只读取自己的目录、配置和凭据。其他机器人、网站、数据库和备份目录默认不开放。

身份文件只记录岗位、行为和汇报关系。它不授予系统控制权。实际权限由目录权限、服务账号、MCP 工具、沙箱和审批共同决定。

权限按动作分开。

- 文件权限只覆盖任务需要的目录。
- 外部账户权限只覆盖指定账户和指定动作。
- 管理员权限按单次任务确认，不作为常驻权限。
- 邮件发送、成员邀请、内容发布和管理员动作保留人工确认。
- 一次管理员确认不允许修改其他服务，也不允许增加新的工具。
- 支付、其他服务密钥和任务以外的配置保持关闭。

日志记录任务编号、动作、时间和结果。日志不保存密钥、确认信息和完整私密正文。出现权限错误时先保留失败记录。没有确认错误来源以前，不通过扩大整机权限来让任务继续运行。

Agent 可以读取定时生成的服务状态快照。快照可以包含在线、停止或失败状态，但不提供启动、停止和修改服务的控制接口。

## 7.9 目录迁移和回滚

迁移前保存配置、服务文件和运行时路径清单。迁移后逐项检查这些位置。

- 脚本开头的解释器路径。
- Python 的 editable install。
- MCP 命令和工作目录。
- systemd 服务文件。
- 环境变量和配置文件。
- 沙箱挂载和允许目录。

旧路径兼容链接和回滚副本要写明保留期限。新路径通过服务启动、工具清单和只读工单检查以后，再由维护者决定是否清理旧副本。
